import 'package:flutter/material.dart';

/// This Widget shows the user a view of the specified Youtube-Channel.
class YoutubeChannelPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Material(
      color: Colors.blue
    );
  }
}